# 給 Codex 的指令

> **2026-09-23 最新：使用者決定整個拿掉紅櫻花蝦。** 立刻停止所有蝦相關工作：`CODEX_FRONTEND_PLAN.md` §3 蝦精緻化整節取消；§2 裡的抱卵卵團、脫殼空殼演出取消；不要再產出蝦的素材或比較圖。已經做到一半的蝦素材先留著、不要整合。其他步驟（事件游標、魚與花園鰻的事件演出、§3.5 花園鰻）照常。Claude 正在和使用者確認後續細節，確認後會更新計畫。

請讀取 `docs/CODEX_FRONTEND_PLAN.md` 並依序執行前端工作；第 0 步收好 commit 之後，最優先做 0.5（修動作卡頓）。規格以 `docs/CODEX_FRONTEND_HANDOFF.md` 為準，backend 欄位以 `docs/BACKEND_SNAPSHOT_EVENTS.md` 為準，美術沿用 `docs/ART_DIRECTION.md` 的 Soft Pixel 魚蝦風格。先把目前還沒 commit 的前端收好，只 commit 自己的檔案。蝦精緻化做完素材和比較圖後要先停下來給使用者看，使用者說好才整合。不要在本機跑長時間模擬。每項結果都要標明已完成、未完成或需要 backend，不要只寫「完成」。
